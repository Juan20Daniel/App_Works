import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        backgroundColor: '#E5E7EB',
        overflow: 'hidden',
        zIndex: 20
    },
    shimmer: {
        position: 'absolute', 
        width:'180%', 
        height:'180%'
    }
});